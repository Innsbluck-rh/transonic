<!--
author: innsbluck
date: 2026-03-30 09:32
version: 0.0.1
-->

- 正直全然ADR書けてない。Codexにも変更の度に書かせるようにしたいし、俺も書かなければ。
- これは編集の後記ではなく、全体像がつかめなくなったためもう一度全体を"読み直す"ことをした後の感想

# 現状について

- 多少の不満(不整合)はあるものの、おそらくSubsonic APIはいい感じにクレート分離できているし、おおむね実装基盤ができているのであろう
- src以下が若干問題か。呼び分けがむずいので、バックエンドにおけるsubsonicAPIの実装を**純粋API層**、それ以上のレベルの複合的なサービスないしコマンド実装を**コマンド層**と呼ぶ。以下の疑問がある。
  - コマンド層の複雑度の幅
    1. 複合的な純粋API層の呼び出しや、Rustでのデータ処理を必要とする単一コマンドもあるだろうが、普通に純粋API層をそのまま呼んで取得するだけでいいコマンドとかがあったりしないだろうか。たしか純粋API層の直接呼出しは許可していない。
    2. コマンドをどう切り出すか、というのはバックエンドコードの可読性やシンプルさを保つために大いに重要だと思われるが、その辺を今完全にCodexに任せちゃっているので不安(疑問というか不安)
  - コマンド層のdoc化の可否
    正直Rustは読みづらい。テストコードまで入っちゃうとなおさらである。Swaggerみたいな視覚的な情報が欲しい。それもテキストで作っちゃうとクソほど整合性を失うはずなのでよくない。自動的にそういうことをする方法、なんかないだろうか？